/node_modules
.snapshots
# profile Chrome bền cho scripts/debug.mjs --profile (chứa cookie/session — không commit)
.chrome-profiles
/.next
/out
*.log
.env*.local

# Telegram bot runtime state (offset + per-chat session ids)
.telegram-state.json
